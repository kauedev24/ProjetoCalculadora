git config --global user.name ""
git config --global user.email ""
git config --global init.defaultBranch main
git init

COM CHAVE SSH
ssh-keygen
copiar diretório onde a chave foi salva ".pub"
acessa diretorio e copia a o código para inserir no github

git remote add origin 
git remote -v -> verificar


problemas com push faça
tentar:

git fetch origin master
git merge origin master
Depois de escrever este código recebi outro erro: (non-fast-forward)

Eu escrevo este código:

git fetch origin master:tmp
git rebase tmp
git push origin HEAD:master
git branch -D tmp
